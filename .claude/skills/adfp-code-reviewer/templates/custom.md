# 项目审查规则

## 审查模式偏好
- 默认模式：{--staged / --path / 完整文件}
- 默认聚焦维度：{all / basic / quality / perf / side-effect}

## 必须通过的检查项（阻塞性）
- {项目特定约束，如："所有 API 调用通过统一封装层"}
- {项目特定约束}

## 忽略的规则
- {不需要检查的规则}

## 严重度调整
- {将某规则的严重度提升或降低}

## 自动修复偏好
- 是否开启自动修复：{是 / 否}
- 排除自动修复的规则：{列出不自动修复的规则}
